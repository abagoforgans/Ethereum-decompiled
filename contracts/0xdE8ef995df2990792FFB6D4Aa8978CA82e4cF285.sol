contract main {


// =======================  Init code  ======================


uint256 stor0;

function _fallback() payable {
    stor0 = msg.sender or Mask(96, 160, stor0)
    return code.data[64 len 690]
}



// =====================  Runtime code  =====================


const getBlockDifficulty = block.difficulty

const getBlockNumber = block.number

const getSender = msg.sender

const sub_60777eb9(?) = address(block.coinbase)

const getBlockTimestamp = block.timestamp

const sub_9402e681(?) = block.gas_limit

const getSenderBalance = eth.balance(msg.sender)


address stor0;

function kill() payable {
    if stor0 != msg.sender:
    selfdestruct(stor0)
}

function _fallback() payable {
  stop
}



}
