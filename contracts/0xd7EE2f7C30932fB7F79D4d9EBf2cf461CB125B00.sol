contract main {


// =======================  Init code  ======================


uint8 stor0;
uint256 stor1;

function _fallback() payable {
    stor0 = 0
    stor1 = block.timestamp
    return code.data[32 len 225]
}



// =====================  Runtime code  =====================


uint8 stage;
uint256 stor0;
uint256 creationTime;

function stage() payable {
    return uint8(stage)
}

function creationTime() payable {
    return creationTime
}

function _fallback() payable {
  stop
}

function h() payable {
    if uint8(stage):
        if 1 == uint8(stage):
            if block.timestamp >= creationTime + (288 * 24 * 3600):
                uint256(stor0) = uint8(stage) + 1 or Mask(248, 8, uint256(stor0))
    else:
        if block.timestamp >= creationTime + (240 * 24 * 3600):
            uint256(stor0) = uint8(stage) + 1 or Mask(248, 8, uint256(stor0))
        if 1 == uint8(stage):
            if block.timestamp >= creationTime + (288 * 24 * 3600):
                uint256(stor0) = uint8(stage) + 1 or Mask(248, 8, uint256(stor0))
}

function g() payable {
    if uint8(stage):
        if 1 == uint8(stage):
            if block.timestamp >= creationTime + (288 * 24 * 3600):
                uint256(stor0) = uint8(stage) + 1 or Mask(248, 8, uint256(stor0))
    else:
        if block.timestamp >= creationTime + (240 * 24 * 3600):
            uint256(stor0) = uint8(stage) + 1 or Mask(248, 8, uint256(stor0))
        if 1 == uint8(stage):
            if block.timestamp >= creationTime + (288 * 24 * 3600):
                uint256(stor0) = uint8(stage) + 1 or Mask(248, 8, uint256(stor0))
}

function i() payable {
    if uint8(stage):
        if 1 == uint8(stage):
            if block.timestamp >= creationTime + (288 * 24 * 3600):
                uint256(stor0) = uint8(stage) + 1 or Mask(248, 8, uint256(stor0))
    else:
        if block.timestamp >= creationTime + (240 * 24 * 3600):
            uint256(stor0) = uint8(stage) + 1 or Mask(248, 8, uint256(stor0))
        if 1 == uint8(stage):
            if block.timestamp >= creationTime + (288 * 24 * 3600):
                uint256(stor0) = uint8(stage) + 1 or Mask(248, 8, uint256(stor0))
}

function bid() payable {
    if uint8(stage):
        if 1 == uint8(stage):
            if block.timestamp >= creationTime + (288 * 24 * 3600):
                uint256(stor0) = uint8(stage) + 1 or Mask(248, 8, uint256(stor0))
    else:
        if block.timestamp >= creationTime + (240 * 24 * 3600):
            uint256(stor0) = uint8(stage) + 1 or Mask(248, 8, uint256(stor0))
        if 1 == uint8(stage):
            if block.timestamp >= creationTime + (288 * 24 * 3600):
                uint256(stor0) = uint8(stage) + 1 or Mask(248, 8, uint256(stor0))
}

function reveal() payable {
    if uint8(stage):
        if 1 == uint8(stage):
            if block.timestamp >= creationTime + (288 * 24 * 3600):
                uint256(stor0) = uint8(stage) + 1 or Mask(248, 8, uint256(stor0))
    else:
        if block.timestamp >= creationTime + (240 * 24 * 3600):
            uint256(stor0) = uint8(stage) + 1 or Mask(248, 8, uint256(stor0))
        if 1 == uint8(stage):
            if block.timestamp >= creationTime + (288 * 24 * 3600):
                uint256(stor0) = uint8(stage) + 1 or Mask(248, 8, uint256(stor0))
}



}
