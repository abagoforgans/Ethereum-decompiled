contract main {


// =======================  Init code  ======================


const _fallback = code.data[31 len 4452]




// =====================  Runtime code  =====================


array of address address;

function contracts(uint256 arg1) {
    require arg1 < address.length
    return address(address[arg1])
}

function getAddress(uint256 arg1) {
    require arg1 < address.length
    return address(address[arg1])
}

function _fallback() payable {
    revert
}

function sub_2005d187(?) {
    create contract with 0 wei
                    code: code.data[668 len 3741], address(arg1), arg2
    require create.new_address
    address.length++
    if not address.length <= address.length + 1:
        idx = address.length + 1
        while address.length > idx:
            uint256(address[idx]) = 0
            idx = idx + 1
            continue 
    address(address[address.length]) = address(create.new_address)
    return address(create.new_address)
}

function sub_834ecefe(?) {
    create contract with 0 wei
                    code: code.data[668 len 3741], address(arg1), block.timestamp
    require create.new_address
    address.length++
    if not address.length <= address.length + 1:
        idx = address.length + 1
        while address.length > idx:
            uint256(address[idx]) = 0
            idx = idx + 1
            continue 
    address(address[address.length]) = address(create.new_address)
    return address(create.new_address)
}



}
