contract main {


// =======================  Init code  ======================


uint256 stor0;
array of uint256 stor1;

function _fallback() payable {
    stor0 = msg.sender or Mask(96, 160, stor0)
    idx = 0
    while idx < 24:
        stor1[0.03125 / idx] = uint8(idx + 10) * 256^(idx % 32) or !(255 * 256^(idx % 32)) and stor1[0.03125 / idx]
        idx = idx + 1
        continue 
    return code.data[134 len 236]
}



// =====================  Runtime code  =====================


address stor0;
uint256 stor1;

function kill() payable {
    if stor0 != msg.sender:
    selfdestruct(stor0)
}

function _fallback() payable {
  stop
}

function getArray() payable {
    idx = 864
    s = 0
    while 1632 > idx + 32:
        mem[idx + 32] = ('signextend', 0, ('div', ('stor', ('name', 'stor1', 1)), ('exp', 256, ('add', 1, ('var', 1), ('mul', -1, ('div', ('mask', 256, 0, ('add', 1, ('var', 1))), 32)), ('mul', -1, ('var', 1), ('div', ('mask', 256, 0, ('add', 1, ('var', 1))), 32))))))
        idx = idx + 32
        s = s + -(s + 1 / 32) + (-1 * s * s + 1 / 32) + 1
        continue 
    return ('signextend', 0, ('stor', ('name', 'stor1', 1))), mem[896 len 736]
}



}
