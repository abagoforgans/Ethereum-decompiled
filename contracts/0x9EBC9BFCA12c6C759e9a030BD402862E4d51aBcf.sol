contract main {


// =======================  Init code  ======================


uint256 stor2;
uint256 stor3;
uint256 stor5;

function _fallback() payable {
    stor2 = 0
    stor3 = 0
    stor5 = msg.sender or Mask(96, 160, stor5)
    return code.data[46 len 973]
}



// =====================  Runtime code  =====================


array of address persons;
uint256 stor1;
uint256 payoutIdx;
uint256 cNiceGuyIdx;
uint256 investor;
address cNiceGuyAddress;

function investor() payable {
    return investor
}

function niceGuys(uint256 arg1) payable {
    require arg1 < stor1
    return address(stor[code.data[941 len 32] + arg1])
}

function cNiceGuyIdx() payable {
    return cNiceGuyIdx
}

function persons(uint256 arg1) payable {
    require arg1 < persons.length
    return address(persons[arg1])
}

function payoutIdx() payable {
    return payoutIdx
}

function cNiceGuy() payable {
    return cNiceGuyAddress
}

function enter() payable {
    require 0 == msg.value
    if investor >= 9:
        if investor != 9:
            if investor != 10:
                # nil
            else:
                require cNiceGuyIdx < stor1
                cNiceGuyAddress = address(stor[code.data[941 len 32] + stor3])
                investor = 0
                cNiceGuyIdx++
                # nil
        else:
            stor1++
            if not stor1 <= stor1 + 1:
                idx = stor1 + code.data[941 len 32] + 1
                while code.data[941 len 32] + stor1 > idx:
                    address(stor[idx]) = 0
                    idx = idx + 1
                    continue 
            require stor1 < stor1
            uint256(stor[code.data[941 len 32] + stor1]) = msg.sender or Mask(96, 160, uint256(stor[code.data[941 len 32] + stor1]))
            investor++
            if investor != 10:
                # nil
            else:
                require cNiceGuyIdx < stor1
                cNiceGuyAddress = address(stor[code.data[941 len 32] + stor3])
                investor = 0
                cNiceGuyIdx++
                # nil
    else:
        persons.length++
        if not persons.length <= persons.length + 1:
            idx = persons.length + 1
            while persons.length > idx:
                address(persons[idx]) = 0
                idx = idx + 1
                continue 
        require persons.length < persons.length
        uint256(persons[persons.length]) = msg.sender or Mask(96, 160, uint256(persons[persons.length]))
        investor++
        if investor != 9:
            if investor != 10:
                if persons.length:
                    call cNiceGuyAddress with:
                         gas 0 wei
                # nil
            else:
                require cNiceGuyIdx < stor1
                cNiceGuyAddress = address(stor[code.data[941 len 32] + stor3])
                investor = 0
                cNiceGuyIdx++
                if persons.length:
                    call cNiceGuyAddress with:
                         gas 0 wei
                # nil
        else:
            stor1++
            if not stor1 <= stor1 + 1:
                idx = stor1 + code.data[941 len 32] + 1
                while code.data[941 len 32] + stor1 > idx:
                    address(stor[idx]) = 0
                    idx = idx + 1
                    continue 
            require stor1 < stor1
            uint256(stor[code.data[941 len 32] + stor1]) = msg.sender or Mask(96, 160, uint256(stor[code.data[941 len 32] + stor1]))
            investor++
            if investor != 10:
                if persons.length:
                    call cNiceGuyAddress with:
                         gas 0 wei
                # nil
            else:
                require cNiceGuyIdx < stor1
                cNiceGuyAddress = address(stor[code.data[941 len 32] + stor3])
                investor = 0
                cNiceGuyIdx++
                if persons.length:
                    call cNiceGuyAddress with:
                         gas 0 wei
                # nil
}

function _fallback() payable {
    require 0 == msg.value
    if investor >= 9:
        if investor != 9:
            if investor != 10:
                # nil
            else:
                require cNiceGuyIdx < stor1
                cNiceGuyAddress = address(stor[code.data[941 len 32] + stor3])
                investor = 0
                cNiceGuyIdx++
                # nil
        else:
            stor1++
            if not stor1 <= stor1 + 1:
                idx = stor1 + code.data[941 len 32] + 1
                while code.data[941 len 32] + stor1 > idx:
                    address(stor[idx]) = 0
                    idx = idx + 1
                    continue 
            require stor1 < stor1
            uint256(stor[code.data[941 len 32] + stor1]) = msg.sender or Mask(96, 160, uint256(stor[code.data[941 len 32] + stor1]))
            investor++
            if investor != 10:
                # nil
            else:
                require cNiceGuyIdx < stor1
                cNiceGuyAddress = address(stor[code.data[941 len 32] + stor3])
                investor = 0
                cNiceGuyIdx++
                # nil
    else:
        persons.length++
        if not persons.length <= persons.length + 1:
            idx = persons.length + 1
            while persons.length > idx:
                address(persons[idx]) = 0
                idx = idx + 1
                continue 
        require persons.length < persons.length
        uint256(persons[persons.length]) = msg.sender or Mask(96, 160, uint256(persons[persons.length]))
        investor++
        if investor != 9:
            if investor != 10:
                if persons.length:
                    call cNiceGuyAddress with:
                         gas 0 wei
                # nil
            else:
                require cNiceGuyIdx < stor1
                cNiceGuyAddress = address(stor[code.data[941 len 32] + stor3])
                investor = 0
                cNiceGuyIdx++
                if persons.length:
                    call cNiceGuyAddress with:
                         gas 0 wei
                # nil
        else:
            stor1++
            if not stor1 <= stor1 + 1:
                idx = stor1 + code.data[941 len 32] + 1
                while code.data[941 len 32] + stor1 > idx:
                    address(stor[idx]) = 0
                    idx = idx + 1
                    continue 
            require stor1 < stor1
            uint256(stor[code.data[941 len 32] + stor1]) = msg.sender or Mask(96, 160, uint256(stor[code.data[941 len 32] + stor1]))
            investor++
            if investor != 10:
                if persons.length:
                    call cNiceGuyAddress with:
                         gas 0 wei
                # nil
            else:
                require cNiceGuyIdx < stor1
                cNiceGuyAddress = address(stor[code.data[941 len 32] + stor3])
                investor = 0
                cNiceGuyIdx++
                if persons.length:
                    call cNiceGuyAddress with:
                         gas 0 wei
                # nil
}



}
