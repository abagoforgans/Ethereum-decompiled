contract main {


// =======================  Init code  ======================


const _fallback = code.data[19 len 544]




// =====================  Runtime code  =====================


uint256 stor0;
address stor1;
uint256 stor1;
address stor2;
uint256 stor2;

function sub_bb4c4974(?) payable {
    uint256(stor2) = msg.sender or Mask(96, 160, uint256(stor2))
    uint256(stor1) = msg.sender or Mask(96, 160, uint256(stor1))
    stor0 = block.number
}

function _fallback() payable {
    if eth.balance(this.address) <= 0:
        if msg.value >= 10^17:
            uint256(stor1) = msg.sender or Mask(96, 160, uint256(stor1))
            stor0 = block.number
    else:
        if block.number - stor0 > 200:
            call address(stor2) with:
               value eth.balance(this.address) / 100 wei
                 gas 0 wei
            call address(stor1) with:
               value eth.balance(this.address) - (eth.balance(this.address) / 100) wei
                 gas 0 wei
        if msg.value >= 10^17:
            uint256(stor1) = msg.sender or Mask(96, 160, uint256(stor1))
            stor0 = block.number
}



}
