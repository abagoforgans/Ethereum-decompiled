contract main {


// =======================  Init code  ======================


const _fallback = code.data[16 len 208]




// =====================  Runtime code  =====================


array of uint256 stor0;
array of uint256 _last;

function get_last() payable {
    require stor0.length - 1 < stor0.length
    return _last[stor0.length]
}

function _fallback() payable {
  stop
}

function put() payable {
    stor0.length++
    if not stor0.length <= stor0.length + 1:
        idx = stor0.length + 1
        while stor0.length > idx:
            stor0[idx] = 0
            idx = idx + 1
            continue 
    require stor0.length < stor0.length
    stor0[stor0.length] = block.timestamp
}



}
