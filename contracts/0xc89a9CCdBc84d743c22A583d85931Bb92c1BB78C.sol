contract main {


// =======================  Init code  ======================


const _fallback = code.data[18 len 808]




// =====================  Runtime code  =====================


array of uint256 hashes;
array of uint256 stor99;

function hashes(uint256 arg1) payable {
    return hashes[arg1][0 len hashes[arg1].length]
}

function sub_8c28c022(?) payable {
    return hashes.length
}

function _fallback() payable {
  stop
}

function publish(string arg1) payable {
    hashes.length++
    if not hashes.length <= hashes.length + 1:
        idx = hashes.length + 0x290decd9548b62a8d60345a988386fc84ba6bc95484008f6362f93160ef3e564
        while hashes.length + 0x290decd9548b62a8d60345a988386fc84ba6bc95484008f6362f93160ef3e563 > idx:
            stor[idx] = 0
            if 31 < stor[idx].length:
                mem[0] = idx
                s = sha3(idx)
                while sha3(idx) + (stor[idx].length + 31 / 32) > s:
                    stor[s] = 0
                    s = s + 1
                    continue 
            idx = idx + 1
            continue 
    require hashes.length - 1 < hashes.length
    stor[sha3(hashes.length + ('name', 'hashes', 0) - 1)][] = Array(len=arg1.length, data=arg1[all])
}



}
