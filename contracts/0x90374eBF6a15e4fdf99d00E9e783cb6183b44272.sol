contract main {


// =======================  Init code  ======================


address stor0;
uint256 stor1;
uint256 stor2;
uint256 stor3;
uint256 stor4;
uint256 stor5;
address stor8;
address stor10;
address stor11;
uint256 stor12;
uint256 stor13;
array of struct stor18;
address stor19;

function _fallback() payable {
    stor1 = 5000 * 10^18
    stor2 = 10^17
    stor3 = 20 * 10^18
    stor4 = 114000 * 10^18
    stor5 = 420373 * 3600
    stor12 = 5
    stor13 = 15
    create contract with 0 wei
                    code: code.data[6279 len 3721]
    require create.new_address
    stor19 = address(create.new_address)
    require not msg.value
    stor0 = msg.sender
    stor4 = 114000 * 10^18
    stor1 = 5000 * 10^18
    stor5 = 420373 * 3600
    stor8 = 0x727436a7e7b836f3ab8d1caf475fafeaeb25ff27
    stor10 = 0x38e4f2a7625a391bfe59d6ac74b26d8556d6361e
    stor11 = 0x76a13d4f571107f363ff253e80706dace889aded
    require stor0 == msg.sender
    stor18.length++
    if not stor18.length <= stor18.length + 1:
        idx = 2 * stor18.length + 1
        while 2 * stor18.length > idx:
            stor18[idx].field_0 = 0
            stor18[idx].field_256 = 0
            idx = idx + 2
            continue 
    stor18[stor18.length].field_0 = 7
    stor18[stor18.length].field_256 = 30
    require stor0 == msg.sender
    stor18.length++
    if not stor18.length <= stor18.length + 1:
        idx = 2 * stor18.length + 1
        while 2 * stor18.length > idx:
            stor18[idx].field_0 = 0
            stor18[idx].field_256 = 0
            idx = idx + 2
            continue 
    stor18[stor18.length].field_0 = 21
    stor18[stor18.length].field_256 = 15
    require stor0 == msg.sender
    stor18.length++
    if not stor18.length <= stor18.length + 1:
        idx = 2 * stor18.length + 1
        while 2 * stor18.length > idx:
            stor18[idx].field_0 = 0
            stor18[idx].field_256 = 0
            idx = idx + 2
            continue 
    stor18[stor18.length].field_0 = 56
    stor18[stor18.length].field_256 = 0
    return code.data[634 len 5645]
}



// =====================  Runtime code  =====================


#
#  - payExtraTokens(uint256 arg1)
#
const PERCENT_RATE = 100


address owner;
uint256 price;
uint256 minInvestedLimit;
uint256 maxInvestedLimit;
uint256 hardcap;
uint256 start;
uint256 invested;
uint256 extraTokensPercent;
address walletAddress;
address directMintAgentAddress;
address bountyTokensWalletAddress;
address foundersTokensWalletAddress;
uint256 bountyTokensPercent;
uint256 foundersTokensPercent;
uint256 index;
uint8 stor15;
uint8 stor15; offset 8
array of address tokenHolders;
mapping of uint256 balances;
array of struct discount;
address tokenAddress;

function balances(address arg1) {
    return balances[arg1]
}

function index() {
    return index
}

function tokenHoldersCount() {
    return tokenHolders.length
}

function foundersTokensWallet() {
    return foundersTokensWalletAddress
}

function wallet() {
    return walletAddress
}

function extraTokensTransferred() {
    return bool(uint8(stor15.field_8))
}

function extraTokensPercent() {
    return extraTokensPercent
}

function directMintAgent() {
    return directMintAgentAddress
}

function isITOFinished() {
    return bool(uint8(stor15.field_0))
}

function foundersTokensPercent() {
    return foundersTokensPercent
}

function owner() {
    return owner
}

function tokenHolders(uint256 arg1) {
    require arg1 < tokenHolders.length
    return address(tokenHolders[arg1])
}

function bountyTokensWallet() {
    return bountyTokensWalletAddress
}

function bountyTokensPercent() {
    return bountyTokensPercent
}

function price() {
    return price
}

function milestonesCount() {
    return discount.length
}

function hardcap() {
    return hardcap
}

function start() {
    return start
}

function invested() {
    return invested
}

function getDiscount() {
    require 0 < discount.length
    s = 0
    s = 0
    t = start
    while block.timestamp >= t + (24 * 3600 * discount[s].field_0):
        require s + 1 < discount.length
        mem[0] = 18
        s = (2 * s) + sha3(18)
        s = s + 1
        t = t + (24 * 3600 * discount[s].field_0)
        continue 
    return discount[s].field_256
}

function minInvestedLimit() {
    return minInvestedLimit
}

function milestones(uint256 arg1) {
    require arg1 < discount.length
    return discount[arg1].field_0, discount[arg1].field_256
}

function maxInvestedLimit() {
    return maxInvestedLimit
}

function token() {
    return tokenAddress
}

function setPrice(uint256 arg1) {
    require owner == msg.sender
    price = arg1
}

function setStart(uint256 arg1) {
    require owner == msg.sender
    start = arg1
}

function setHardcap(uint256 arg1) {
    require owner == msg.sender
    hardcap = arg1
}

function setWallet(address arg1) {
    require owner == msg.sender
    walletAddress = arg1
}

function setMaxInvestedLimit(uint256 arg1) {
    require owner == msg.sender
    maxInvestedLimit = arg1
}

function setMinInvestedLimit(uint256 arg1) {
    require owner == msg.sender
    minInvestedLimit = arg1
}

function setExtraTokensPercent(uint256 arg1) {
    require owner == msg.sender
    extraTokensPercent = arg1
}

function setDirectMintAgent(address arg1) {
    require owner == msg.sender
    directMintAgentAddress = arg1
}

function setBountyTokensPercent(uint256 arg1) {
    require owner == msg.sender
    bountyTokensPercent = arg1
}

function setFoundersTokensPercent(uint256 arg1) {
    require owner == msg.sender
    foundersTokensPercent = arg1
}

function setBountyTokensWallet(address arg1) {
    require owner == msg.sender
    bountyTokensWalletAddress = arg1
}

function setFoundersTokensWallet(address arg1) {
    require owner == msg.sender
    foundersTokensWalletAddress = arg1
}

function transferOwnership(address arg1) {
    require owner == msg.sender
    require arg1
    emit OwnershipTransferred(owner, arg1);
    owner = arg1
}

function unlock(address arg1) {
    require owner == msg.sender
    require ext_code.size(tokenAddress)
    call tokenAddress.0x2f6c493c with:
         gas gas_remaining - 710 wei
        args arg1
    require ext_call.success
}

function retrieveTokens(address arg1) {
    require owner == msg.sender
    require ext_code.size(arg1)
    call arg1.0x70a08231 with:
         gas gas_remaining - 710 wei
        args this.address
    require ext_call.success
    require ext_code.size(arg1)
    call arg1.0xa9059cbb with:
         gas gas_remaining - 710 wei
        args walletAddress, ext_call.return_data[0]
    require ext_call.success
}

function tokenOperationsFinished() {
    require owner == msg.sender
    require uint8(stor15.field_8)
    require ext_code.size(tokenAddress)
    call tokenAddress.0x7d64bcb4 with:
         gas gas_remaining - 710 wei
    require ext_call.success
    require ext_code.size(tokenAddress)
    call tokenAddress.0xf2fde38b with:
         gas gas_remaining - 710 wei
        args owner
    require ext_call.success
}

function addMilestone(uint256 arg1, uint256 arg2) {
    require owner == msg.sender
    discount.length++
    if not discount.length <= discount.length + 1:
        idx = 2 * discount.length + 1
        while 2 * discount.length > idx:
            discount[idx].field_0 = 0
            discount[idx].field_256 = 0
            idx = idx + 2
            continue 
    discount[discount.length].field_0 = arg1
    discount[discount.length].field_256 = arg2
}

function finishITO() {
    require owner == msg.sender
    require not uint8(stor15.field_0)
    require foundersTokensPercent + bountyTokensPercent >= bountyTokensPercent
    require ext_code.size(tokenAddress)
    call tokenAddress.0x18160ddd with:
         gas gas_remaining - 710 wei
    require ext_call.success
    require foundersTokensPercent + bountyTokensPercent <= 100
    if ext_call.return_data[0]:
        require ext_call.return_data[0]
        require 100 * ext_call.return_data[0] / ext_call.return_data[0] == 100
    require -foundersTokensPercent + -bountyTokensPercent + 100
    if 100 * ext_call.return_data[0] / -foundersTokensPercent + -bountyTokensPercent + 100:
        require 100 * ext_call.return_data[0] / -foundersTokensPercent + -bountyTokensPercent + 100
        require bountyTokensPercent * 100 * ext_call.return_data[0] / -foundersTokensPercent + -bountyTokensPercent + 100 / 100 * ext_call.return_data[0] / -foundersTokensPercent + -bountyTokensPercent + 100 == bountyTokensPercent
    require ext_code.size(tokenAddress)
    call tokenAddress.0x70a08231 with:
         gas gas_remaining - 710 wei
        args bountyTokensWalletAddress
    require ext_call.success
    if not ext_call.return_data[0]:
        tokenHolders.length++
        if not tokenHolders.length <= tokenHolders.length + 1:
            idx = tokenHolders.length + 1
            while tokenHolders.length > idx:
                uint256(tokenHolders[idx]) = 0
                idx = idx + 1
                continue 
        address(tokenHolders[tokenHolders.length]) = bountyTokensWalletAddress
    require ext_code.size(tokenAddress)
    call tokenAddress.0x40c10f19 with:
         gas gas_remaining - 710 wei
        args bountyTokensWalletAddress, bountyTokensPercent * 100 * ext_call.return_data[0] / -foundersTokensPercent + -bountyTokensPercent + 100 / 100
    require ext_call.success
    if 100 * ext_call.return_data[0] / -foundersTokensPercent + -bountyTokensPercent + 100:
        require 100 * ext_call.return_data[0] / -foundersTokensPercent + -bountyTokensPercent + 100
        require foundersTokensPercent * 100 * ext_call.return_data[0] / -foundersTokensPercent + -bountyTokensPercent + 100 / 100 * ext_call.return_data[0] / -foundersTokensPercent + -bountyTokensPercent + 100 == foundersTokensPercent
    require ext_code.size(tokenAddress)
    call tokenAddress.0x70a08231 with:
         gas gas_remaining - 710 wei
        args foundersTokensWalletAddress
    require ext_call.success
    if not ext_call.return_data[0]:
        tokenHolders.length++
        if not tokenHolders.length <= tokenHolders.length + 1:
            idx = tokenHolders.length + 1
            while tokenHolders.length > idx:
                uint256(tokenHolders[idx]) = 0
                idx = idx + 1
                continue 
        address(tokenHolders[tokenHolders.length]) = foundersTokensWalletAddress
    require ext_code.size(tokenAddress)
    call tokenAddress.0x40c10f19 with:
         gas gas_remaining - 710 wei
        args foundersTokensWalletAddress, foundersTokensPercent * 100 * ext_call.return_data[0] / -foundersTokensPercent + -bountyTokensPercent + 100 / 100
    require ext_call.success
    uint8(stor15.field_0) = 1
}

function end() {
    s = 0
    idx = 0
    s = start
    while idx < discount.length:
        mem[0] = 18
        s = (2 * idx) + sha3(18)
        idx = idx + 1
        s = s + (24 * 3600 * discount[idx].field_0)
        continue 
    return (start + (start * discount.length) + (start * discount.length * discount.length) + (start * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (s * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length))
}

function directMint(address arg1, uint256 arg2) {
    if directMintAgentAddress != msg.sender:
        require owner == msg.sender
    require arg2 >= minInvestedLimit
    require block.timestamp >= start
    s = 0
    idx = 0
    s = start
    while idx < discount.length:
        mem[0] = 18
        s = (2 * idx) + sha3(18)
        idx = idx + 1
        s = s + (24 * 3600 * discount[idx].field_0)
        continue 
    require block.timestamp < start + (start * discount.length) + (start * discount.length * discount.length) + (start * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (s * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length)
    require invested < hardcap
    require msg.value + invested >= invested
    invested += msg.value
    require 0 < discount.length
    s = 0
    s = 0
    t = start
    while block.timestamp >= t + (24 * 3600 * discount[s].field_0):
        require s + 1 < discount.length
        mem[0] = 18
        s = (2 * s) + sha3(18)
        s = s + 1
        t = t + (24 * 3600 * discount[s].field_0)
        continue 
    require discount[s].field_256 <= 100
    if price:
        require price
        require 100 * price / price == 100
    if arg2:
        require arg2
        require 100 * price * arg2 / arg2 == 100 * price
    require -discount[s].field_256 + 100
    require ext_code.size(tokenAddress)
    call tokenAddress.0x70a08231 with:
         gas gas_remaining - 710 wei
        args arg1
    require ext_call.success
    if not ext_call.return_data[0]:
        tokenHolders.length++
        if not tokenHolders.length <= tokenHolders.length + 1:
            idx = tokenHolders.length + 1
            while tokenHolders.length > idx:
                uint256(tokenHolders[idx]) = 0
                idx = idx + 1
                continue 
        address(tokenHolders[tokenHolders.length]) = arg1
    require ext_code.size(tokenAddress)
    call tokenAddress.0x40c10f19 with:
         gas gas_remaining - 710 wei
        args address(arg1), 100 * price * arg2 / -discount[s].field_256 + 100 / 10^18
    require ext_call.success
    require arg2 + balances[address(arg1)] >= balances[address(arg1)]
    balances[address(arg1)] += arg2
    if arg2 + balances[address(arg1)] >= maxInvestedLimit:
        require ext_code.size(tokenAddress)
        call tokenAddress.lock(address rg1) with:
             gas gas_remaining - 710 wei
            args arg1
        require ext_call.success
}

function _fallback() payable {
    require msg.value >= minInvestedLimit
    require block.timestamp >= start
    s = 0
    idx = 0
    s = start
    while idx < discount.length:
        mem[0] = 18
        s = (2 * idx) + sha3(18)
        idx = idx + 1
        s = s + (24 * 3600 * discount[idx].field_0)
        continue 
    require block.timestamp < start + (start * discount.length) + (start * discount.length * discount.length) + (start * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (s * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length)
    require invested < hardcap
    require not uint8(stor15.field_0)
    call walletAddress with:
       value msg.value wei
         gas 2300 * is_zero(value) wei
    require ext_call.success
    require msg.value + invested >= invested
    invested += msg.value
    require 0 < discount.length
    s = 0
    s = 0
    t = start
    while block.timestamp >= t + (24 * 3600 * discount[s].field_0):
        require s + 1 < discount.length
        mem[0] = 18
        s = (2 * s) + sha3(18)
        s = s + 1
        t = t + (24 * 3600 * discount[s].field_0)
        continue 
    require discount[s].field_256 <= 100
    if price:
        require price
        require 100 * price / price == 100
    if msg.value:
        require msg.value
        require 100 * price * msg.value / msg.value == 100 * price
    require -discount[s].field_256 + 100
    require ext_code.size(tokenAddress)
    call tokenAddress.0x70a08231 with:
         gas gas_remaining - 710 wei
        args msg.sender
    require ext_call.success
    if not ext_call.return_data[0]:
        tokenHolders.length++
        if not tokenHolders.length <= tokenHolders.length + 1:
            idx = tokenHolders.length + 1
            while tokenHolders.length > idx:
                uint256(tokenHolders[idx]) = 0
                idx = idx + 1
                continue 
        address(tokenHolders[tokenHolders.length]) = msg.sender
    require ext_code.size(tokenAddress)
    call tokenAddress.0x40c10f19 with:
         gas gas_remaining - 710 wei
        args msg.sender, 100 * price * msg.value / -discount[s].field_256 + 100 / 10^18
    require ext_call.success
    require msg.value + balances[address(msg.sender)] >= balances[address(msg.sender)]
    balances[address(msg.sender)] += msg.value
    if msg.value + balances[address(msg.sender)] >= maxInvestedLimit:
        require ext_code.size(tokenAddress)
        call tokenAddress.lock(address rg1) with:
             gas gas_remaining - 710 wei
            args msg.sender
        require ext_call.success
}

function createTokens() payable {
    require msg.value >= minInvestedLimit
    require block.timestamp >= start
    s = 0
    idx = 0
    s = start
    while idx < discount.length:
        mem[0] = 18
        s = (2 * idx) + sha3(18)
        idx = idx + 1
        s = s + (24 * 3600 * discount[idx].field_0)
        continue 
    require block.timestamp < start + (start * discount.length) + (start * discount.length * discount.length) + (start * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (start * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length) + (s * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length * discount.length)
    require invested < hardcap
    require not uint8(stor15.field_0)
    call walletAddress with:
       value msg.value wei
         gas 2300 * is_zero(value) wei
    require ext_call.success
    require msg.value + invested >= invested
    invested += msg.value
    require 0 < discount.length
    s = 0
    s = 0
    t = start
    while block.timestamp >= t + (24 * 3600 * discount[s].field_0):
        require s + 1 < discount.length
        mem[0] = 18
        s = (2 * s) + sha3(18)
        s = s + 1
        t = t + (24 * 3600 * discount[s].field_0)
        continue 
    require discount[s].field_256 <= 100
    if price:
        require price
        require 100 * price / price == 100
    if msg.value:
        require msg.value
        require 100 * price * msg.value / msg.value == 100 * price
    require -discount[s].field_256 + 100
    require ext_code.size(tokenAddress)
    call tokenAddress.0x70a08231 with:
         gas gas_remaining - 710 wei
        args msg.sender
    require ext_call.success
    if not ext_call.return_data[0]:
        tokenHolders.length++
        if not tokenHolders.length <= tokenHolders.length + 1:
            idx = tokenHolders.length + 1
            while tokenHolders.length > idx:
                uint256(tokenHolders[idx]) = 0
                idx = idx + 1
                continue 
        address(tokenHolders[tokenHolders.length]) = msg.sender
    require ext_code.size(tokenAddress)
    call tokenAddress.0x40c10f19 with:
         gas gas_remaining - 710 wei
        args msg.sender, 100 * price * msg.value / -discount[s].field_256 + 100 / 10^18
    require ext_call.success
    require msg.value + balances[address(msg.sender)] >= balances[address(msg.sender)]
    balances[address(msg.sender)] += msg.value
    if msg.value + balances[address(msg.sender)] >= maxInvestedLimit:
        require ext_code.size(tokenAddress)
        call tokenAddress.lock(address rg1) with:
             gas gas_remaining - 710 wei
            args msg.sender
        require ext_call.success
}



}
