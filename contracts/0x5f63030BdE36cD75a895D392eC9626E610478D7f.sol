contract main {


// =======================  Init code  ======================


const _fallback = code.data[18 len 7858]




// =====================  Runtime code  =====================


function _fallback() payable {
  stop
}

function buildDSMap() payable {
    create contract with 0 wei
                    code: code.data[4891 len 1231]
    call address(create.new_address).updateAuthority(address rg1, uint8 rg2) with:
         gas gas_remaining - 25050 wei
        args msg.sender, 0
    require ext_call.success
    return address(create.new_address)
}

function buildDSNullMap() payable {
    create contract with 0 wei
                    code: code.data[6122 len 1736]
    call address(create.new_address).updateAuthority(address rg1, uint8 rg2) with:
         gas gas_remaining - 25050 wei
        args msg.sender, 0
    require ext_call.success
    return address(create.new_address)
}

function buildDSBalanceDB() payable {
    create contract with 0 wei
                    code: code.data[643 len 2817]
    call address(create.new_address).updateAuthority(address rg1, uint8 rg2) with:
         gas gas_remaining - 25050 wei
        args msg.sender, 0
    require ext_call.success
    return address(create.new_address)
}

function buildDSApprovalDB() payable {
    create contract with 0 wei
                    code: code.data[3460 len 1431]
    call address(create.new_address).updateAuthority(address rg1, uint8 rg2) with:
         gas gas_remaining - 25050 wei
        args msg.sender, 0
    require ext_call.success
    return address(create.new_address)
}



}
