contract main {




// =====================  Runtime code  =====================


function _fallback() payable {
    revert
}

function random(uint256 arg1, uint256 arg2) {
    return sha3(block.hash(arg1), arg2)
}

function sub_20e9b097(?) {
    require block.timestamp
    require block.timestamp
    if gas_remaining + (sha3(address(msg.sender, 0)) / block.timestamp) < sha3(address(msg.sender, 0)) / block.timestamp:
        revert with 0, 'SafeMath add failed'
    if arg2 < 0:
        revert with 0, 'SafeMath add failed'
    if sha3(block.hash(block.number - 1)) / block.timestamp < 0:
        revert with 0, 'SafeMath add failed'
    if sha3((sha3(block.hash(block.number - 1)) / block.timestamp) + arg2 + gas_remaining + (sha3(address(msg.sender, 0)) / block.timestamp)) + arg1 >= arg1:
        return (sha3((sha3(block.hash(block.number - 1)) / block.timestamp) + arg2 + gas_remaining + (sha3(address(msg.sender, 0)) / block.timestamp)) + arg1)
    return sha3((sha3(block.hash(block.number - 1)) / block.timestamp) + arg2 + gas_remaining + (sha3(address(msg.sender, 0)) / block.timestamp))
}



}
