contract main {


// =======================  Init code  ======================


address stor0;
address stor1;
address stor2;
uint256 stor3;
uint256 stor4;
address stor5;

function _fallback() {
    stor0 = code.data[17262 len 20]
    stor1 = code.data[17294 len 20]
    stor2 = code.data[17326 len 20]
    stor3 = code.data[17346 len 32]
    stor4 = stor3 + (672 * 24 * 3600)
    create contract with 0 wei
                    code: code.data[11967 len 5283], address(this.address), stor0, stor3, stor4
    require create.new_address
    stor5 = address(create.new_address)
    require ext_code.size(stor5)
    call stor5.MAX_TOTAL_TOKEN_AMOUNT_OFFERED_TO_PUBLIC() with:
         gas gas_remaining - 50 wei
    require ext_call.success
    require ext_code.size(stor5)
    call stor5.mintLiquidToken(address rg1, uint256 rg2) with:
         gas gas_remaining - 50 wei
        args stor0, 1000 * ext_call.return_data[0] / 10000
    require ext_call.success
    require ext_code.size(stor5)
    call stor5.mintIcedToken(address rg1, uint256 rg2) with:
         gas gas_remaining - 50 wei
        args 0x8cb08267c381d6339cab49b7bafacc9ce5a503a0, 445 * ext_call.return_data[0] / 10000
    require ext_call.success
    require ext_code.size(stor5)
    call stor5.mintIcedToken(address rg1, uint256 rg2) with:
         gas gas_remaining - 50 wei
        args 0xbce173c414fe96f0f282e1db904fd4d45eee9e05, 445 * ext_call.return_data[0] / 10000
    require ext_call.success
    require ext_code.size(stor5)
    call stor5.mintIcedToken(address rg1, uint256 rg2) with:
         gas gas_remaining - 50 wei
        args 0x779e0e4c6083cfd26de77b4dbc107a7ebb99d2, 300 * ext_call.return_data[0] / 10000
    require ext_call.success
    require ext_code.size(stor5)
    call stor5.mintIcedToken(address rg1, uint256 rg2) with:
         gas gas_remaining - 50 wei
        args 0x1f06b976136e94704d328d4d23aae7259aac12a2, 100 * ext_call.return_data[0] / 10000
    require ext_call.success
    require ext_code.size(stor5)
    call stor5.mintIcedToken(address rg1, uint256 rg2) with:
         gas gas_remaining - 50 wei
        args 0xdd91615ea8de94bc48231c4ae9488891f1648dc5, 50 * ext_call.return_data[0] / 10000
    require ext_call.success
    require ext_code.size(stor5)
    call stor5.mintIcedToken(address rg1, uint256 rg2) with:
         gas gas_remaining - 50 wei
        args 0x4f2af8d2614190cc80c6e9772b0c367db8d9753c, 50 * ext_call.return_data[0] / 10000
    require ext_call.success
    require ext_code.size(stor5)
    call stor5.mintIcedToken(address rg1, uint256 rg2) with:
         gas gas_remaining - 50 wei
        args 0x715a70a7c7d76acc8d5874862e381c1940c19cce, 25 * ext_call.return_data[0] / 10000
    require ext_call.success
    require ext_code.size(stor5)
    call stor5.mintIcedToken(address rg1, uint256 rg2) with:
         gas gas_remaining - 50 wei
        args 0x8615f13c12c24dfdca0ba32511e2861be02b93b2, 10 * ext_call.return_data[0] / 10000
    require ext_call.success
    require ext_code.size(stor5)
    call stor5.mintIcedToken(address rg1, uint256 rg2) with:
         gas gas_remaining - 50 wei
        args 0xd3841fb80ce408ca7d0b41d72aa91ca74652af47, 5 * ext_call.return_data[0] / 10000
    require ext_call.success
    require ext_code.size(stor5)
    call stor5.mintIcedToken(address rg1, uint256 rg2) with:
         gas gas_remaining - 50 wei
        args 0xdb775577538018a689e4ad2e8eb5a7ae7c34722b, 5 * ext_call.return_data[0] / 10000
    require ext_call.success
    require ext_code.size(stor5)
    call stor5.mintIcedToken(address rg1, uint256 rg2) with:
         gas gas_remaining - 50 wei
        args 0xaa967e0ce6a1ff5f9c124d15ad0412f137c99767, 5 * ext_call.return_data[0] / 10000
    require ext_call.success
    require ext_code.size(stor5)
    call stor5.mintIcedToken(address rg1, uint256 rg2) with:
         gas gas_remaining - 50 wei
        args 0x910b41a6568a645437bc286a5c733f3c501d8c88, 5 * ext_call.return_data[0] / 10000
    require ext_call.success
    require ext_code.size(stor5)
    call stor5.mintIcedToken(address rg1, uint256 rg2) with:
         gas gas_remaining - 50 wei
        args 0xb1d16bfe840e66e3c81785551832aaacb4cf69f3, 5 * ext_call.return_data[0] / 10000
    require ext_call.success
    require ext_code.size(stor5)
    call stor5.mintIcedToken(address rg1, uint256 rg2) with:
         gas gas_remaining - 50 wei
        args 0x5f6ff16364bfef546270325695b6e90cc89c497a, 5 * ext_call.return_data[0] / 10000
    require ext_call.success
    require ext_code.size(stor5)
    call stor5.mintIcedToken(address rg1, uint256 rg2) with:
         gas gas_remaining - 50 wei
        args 0x58656e8872b0d266c2accd276cd23f4c0b5fefb9, 5 * ext_call.return_data[0] / 10000
    require ext_call.success
    require ext_code.size(stor5)
    call stor5.mintIcedToken(address rg1, uint256 rg2) with:
         gas gas_remaining - 50 wei
        args 0x8a815e818e617d1f93be7477d179258ac2d25310, 25 * ext_call.return_data[0] / 10000
    require ext_call.success
    require ext_code.size(stor5)
    call stor5.mintIcedToken(address rg1, uint256 rg2) with:
         gas gas_remaining - 50 wei
        args 0x1eba6702ba21cfc1f6c87c726364b60a5e444901, 10 * ext_call.return_data[0] / 10000
    require ext_call.success
    require ext_code.size(stor5)
    call stor5.mintIcedToken(address rg1, uint256 rg2) with:
         gas gas_remaining - 50 wei
        args 211, 5 * ext_call.return_data[0] / 10000
    require ext_call.success
    return code.data[5221 len 6746]
}



// =====================  Runtime code  =====================


const BTCS_ETHER_CAP = 55000 * 10^18

const AMBASSADOR_STAKE = 5

const EXT_COMPANY_STAKE_THREE = 50

const DIVISOR_PRICE = 1000

const SPECIALIST_THREE = 211

const FOUNDER_ONE = 0x8cb08267c381d6339cab49b7bafacc9ce5a503a0

const EXT_COMPANY_STAKE_TWO = 100

const EXT_COMPANY_ONE = 0x779e0e4c6083cfd26de77b4dbc107a7ebb99d2

const FOUNDER_STAKE = 445

const DIVISOR_STAKE = 10000

const EXT_COMPANY_STAKE_ONE = 300

const SPECIALIST_STAKE_THREE = 5

const ADVISOR_STAKE_TWO = 25

const AMBASSADOR_SEVEN = 0x58656e8872b0d266c2accd276cd23f4c0b5fefb9

const FOUNDER_TWO = 0xbce173c414fe96f0f282e1db904fd4d45eee9e05

const ADVISOR_STAKE_ONE = 50

const EXT_COMPANY_THREE = 0xdd91615ea8de94bc48231c4ae9488891f1648dc5

const AMBASSADOR_TWO = 0xdb775577538018a689e4ad2e8eb5a7ae7c34722b

const PRICE_RATE_SECOND = 2200

const SPECIALIST_STAKE_ONE = 25

const PRICE_RATE_THIRD = 2150

const AMBASSADOR_SIX = 0x5f6ff16364bfef546270325695b6e90cc89c497a

const MELONPORT_COMPANY_STAKE = 1000

const SPECIALIST_STAKE_TWO = 10

const ADVISOR_STAKE_THREE = 10

const EXT_COMPANY_TWO = 0x1f06b976136e94704d328d4d23aae7259aac12a2

const ADVISOR_ONE = 0x4f2af8d2614190cc80c6e9772b0c367db8d9753c

const SPECIALIST_ONE = 0x8a815e818e617d1f93be7477d179258ac2d25310

const SPECIALIST_TWO = 0x1eba6702ba21cfc1f6c87c726364b60a5e444901

const PRICE_RATE_FIRST = 2250

const ADVISOR_THREE = 0x8615f13c12c24dfdca0ba32511e2861be02b93b2

const ADVISOR_TWO = 0x715a70a7c7d76acc8d5874862e381c1940c19cce

const AMBASSADOR_THREE = 0xaa967e0ce6a1ff5f9c124d15ad0412f137c99767

const AMBASSADOR_ONE = 0xd3841fb80ce408ca7d0b41d72aa91ca74652af47

const ETHER_CAP = 220000 * 10^18

const MAX_CONTRIBUTION_DURATION = (672 * 24 * 3600)

const PRICE_RATE_FOURTH = 2100

const AMBASSADOR_FOUR = 0x910b41a6568a645437bc286a5c733f3c501d8c88

const AMBASSADOR_FIVE = 0xb1d16bfe840e66e3c81785551832aaacb4cf69f3


address melonportAddress;
address btcsAddress;
address signerAddress;
uint256 startTime;
uint256 endTime;
address melonTokenAddress;
uint256 etherRaised;
uint8 stor7;

function signer() {
    return signerAddress
}

function endTime() {
    return endTime
}

function melonToken() {
    return melonTokenAddress
}

function startTime() {
    return startTime
}

function halted() {
    return bool(stor7)
}

function btcs() {
    return btcsAddress
}

function etherRaised() {
    return etherRaised
}

function melonport() {
    return melonportAddress
}

function _fallback() payable {
    revert
}

function halt() {
    require msg.sender == melonportAddress
    stor7 = 1
}

function unhalt() {
    require msg.sender == melonportAddress
    stor7 = 0
}

function changeMelonportAddress(address arg1) {
    require msg.sender == melonportAddress
    melonportAddress = arg1
}

function priceRate() {
    if startTime <= block.timestamp:
        if block.timestamp < startTime + (168 * 24 * 3600):
            return 2250
    if startTime + (168 * 24 * 3600) <= block.timestamp:
        if block.timestamp < startTime + (336 * 24 * 3600):
            return 2200
    if startTime + (336 * 24 * 3600) <= block.timestamp:
        if block.timestamp < startTime + (504 * 24 * 3600):
            return 2150
    require startTime + (504 * 24 * 3600) <= block.timestamp
    require block.timestamp < endTime
    return 2100
}

function btcsBuyRecipient(address arg1) payable {
    require msg.sender == btcsAddress
    require block.timestamp < startTime
    require not stor7
    require etherRaised + msg.value >= etherRaised
    require etherRaised + msg.value >= msg.value
    require etherRaised + msg.value <= 55000 * 10^18
    if msg.value:
        require msg.value
        require 2250 * msg.value / msg.value == 2250
    require ext_code.size(melonTokenAddress)
    call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
         gas gas_remaining - 50 wei
        args address(arg1), 2250 * msg.value / 1000
    require ext_call.success
    require etherRaised + msg.value >= etherRaised
    require etherRaised + msg.value >= msg.value
    etherRaised += msg.value
    call melonportAddress with:
       value msg.value wei
         gas 2300 * is_zero(value) wei
    require ext_call.success
    emit TokensBought(msg.value, 2250 * msg.value / 1000, arg1);
}

function buyRecipient(address arg1, uint8 arg2, bytes32 arg3, bytes32 arg4) payable {
    hash = sha256hash(msg.sender) 
    require sha256hash.result
    signer = erecover(hash, arg2 << 248, arg3, arg4) 
    require erecover.result
    require address(signer) == signerAddress
    require block.timestamp >= startTime
    require block.timestamp < endTime
    require not stor7
    require etherRaised + msg.value >= etherRaised
    require etherRaised + msg.value >= msg.value
    require etherRaised + msg.value <= 220000 * 10^18
    if startTime > block.timestamp:
        if startTime + (168 * 24 * 3600) > block.timestamp:
            if startTime + (336 * 24 * 3600) > block.timestamp:
                require startTime + (504 * 24 * 3600) <= block.timestamp
                require block.timestamp < endTime
                if msg.value:
                    require msg.value
                    require 2100 * msg.value / msg.value == 2100
                require ext_code.size(melonTokenAddress)
                call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                     gas gas_remaining - 50 wei
                    args address(arg1), 2100 * msg.value / 1000
                require ext_call.success
                require etherRaised + msg.value >= etherRaised
                require etherRaised + msg.value >= msg.value
                etherRaised += msg.value
                call melonportAddress with:
                   value msg.value wei
                     gas 2300 * is_zero(value) wei
                require ext_call.success
                emit TokensBought(msg.value, 2100 * msg.value / 1000, arg1);
            else:
                if block.timestamp < startTime + (504 * 24 * 3600):
                    if msg.value:
                        require msg.value
                        require 2150 * msg.value / msg.value == 2150
                    require ext_code.size(melonTokenAddress)
                    call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                         gas gas_remaining - 50 wei
                        args address(arg1), 2150 * msg.value / 1000
                    require ext_call.success
                    require etherRaised + msg.value >= etherRaised
                    require etherRaised + msg.value >= msg.value
                    etherRaised += msg.value
                    call melonportAddress with:
                       value msg.value wei
                         gas 2300 * is_zero(value) wei
                    require ext_call.success
                    emit TokensBought(msg.value, 2150 * msg.value / 1000, arg1);
                else:
                    require startTime + (504 * 24 * 3600) <= block.timestamp
                    require block.timestamp < endTime
                    if msg.value:
                        require msg.value
                        require 2100 * msg.value / msg.value == 2100
                    require ext_code.size(melonTokenAddress)
                    call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                         gas gas_remaining - 50 wei
                        args address(arg1), 2100 * msg.value / 1000
                    require ext_call.success
                    require etherRaised + msg.value >= etherRaised
                    require etherRaised + msg.value >= msg.value
                    etherRaised += msg.value
                    call melonportAddress with:
                       value msg.value wei
                         gas 2300 * is_zero(value) wei
                    require ext_call.success
                    emit TokensBought(msg.value, 2100 * msg.value / 1000, arg1);
        else:
            if block.timestamp < startTime + (336 * 24 * 3600):
                if msg.value:
                    require msg.value
                    require 2200 * msg.value / msg.value == 2200
                require ext_code.size(melonTokenAddress)
                call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                     gas gas_remaining - 50 wei
                    args address(arg1), 2200 * msg.value / 1000
                require ext_call.success
                require etherRaised + msg.value >= etherRaised
                require etherRaised + msg.value >= msg.value
                etherRaised += msg.value
                call melonportAddress with:
                   value msg.value wei
                     gas 2300 * is_zero(value) wei
                require ext_call.success
                emit TokensBought(msg.value, 2200 * msg.value / 1000, arg1);
            else:
                if startTime + (336 * 24 * 3600) > block.timestamp:
                    require startTime + (504 * 24 * 3600) <= block.timestamp
                    require block.timestamp < endTime
                    if msg.value:
                        require msg.value
                        require 2100 * msg.value / msg.value == 2100
                    require ext_code.size(melonTokenAddress)
                    call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                         gas gas_remaining - 50 wei
                        args address(arg1), 2100 * msg.value / 1000
                    require ext_call.success
                    require etherRaised + msg.value >= etherRaised
                    require etherRaised + msg.value >= msg.value
                    etherRaised += msg.value
                    call melonportAddress with:
                       value msg.value wei
                         gas 2300 * is_zero(value) wei
                    require ext_call.success
                    emit TokensBought(msg.value, 2100 * msg.value / 1000, arg1);
                else:
                    if block.timestamp < startTime + (504 * 24 * 3600):
                        if msg.value:
                            require msg.value
                            require 2150 * msg.value / msg.value == 2150
                        require ext_code.size(melonTokenAddress)
                        call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                             gas gas_remaining - 50 wei
                            args address(arg1), 2150 * msg.value / 1000
                        require ext_call.success
                        require etherRaised + msg.value >= etherRaised
                        require etherRaised + msg.value >= msg.value
                        etherRaised += msg.value
                        call melonportAddress with:
                           value msg.value wei
                             gas 2300 * is_zero(value) wei
                        require ext_call.success
                        emit TokensBought(msg.value, 2150 * msg.value / 1000, arg1);
                    else:
                        require startTime + (504 * 24 * 3600) <= block.timestamp
                        require block.timestamp < endTime
                        if msg.value:
                            require msg.value
                            require 2100 * msg.value / msg.value == 2100
                        require ext_code.size(melonTokenAddress)
                        call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                             gas gas_remaining - 50 wei
                            args address(arg1), 2100 * msg.value / 1000
                        require ext_call.success
                        require etherRaised + msg.value >= etherRaised
                        require etherRaised + msg.value >= msg.value
                        etherRaised += msg.value
                        call melonportAddress with:
                           value msg.value wei
                             gas 2300 * is_zero(value) wei
                        require ext_call.success
                        emit TokensBought(msg.value, 2100 * msg.value / 1000, arg1);
    else:
        if block.timestamp < startTime + (168 * 24 * 3600):
            if msg.value:
                require msg.value
                require 2250 * msg.value / msg.value == 2250
            require ext_code.size(melonTokenAddress)
            call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                 gas gas_remaining - 50 wei
                args address(arg1), 2250 * msg.value / 1000
            require ext_call.success
            require etherRaised + msg.value >= etherRaised
            require etherRaised + msg.value >= msg.value
            etherRaised += msg.value
            call melonportAddress with:
               value msg.value wei
                 gas 2300 * is_zero(value) wei
            require ext_call.success
            emit TokensBought(msg.value, 2250 * msg.value / 1000, arg1);
        else:
            if startTime + (168 * 24 * 3600) > block.timestamp:
                if startTime + (336 * 24 * 3600) > block.timestamp:
                    require startTime + (504 * 24 * 3600) <= block.timestamp
                    require block.timestamp < endTime
                    if msg.value:
                        require msg.value
                        require 2100 * msg.value / msg.value == 2100
                    require ext_code.size(melonTokenAddress)
                    call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                         gas gas_remaining - 50 wei
                        args address(arg1), 2100 * msg.value / 1000
                    require ext_call.success
                    require etherRaised + msg.value >= etherRaised
                    require etherRaised + msg.value >= msg.value
                    etherRaised += msg.value
                    call melonportAddress with:
                       value msg.value wei
                         gas 2300 * is_zero(value) wei
                    require ext_call.success
                    emit TokensBought(msg.value, 2100 * msg.value / 1000, arg1);
                else:
                    if block.timestamp < startTime + (504 * 24 * 3600):
                        if msg.value:
                            require msg.value
                            require 2150 * msg.value / msg.value == 2150
                        require ext_code.size(melonTokenAddress)
                        call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                             gas gas_remaining - 50 wei
                            args address(arg1), 2150 * msg.value / 1000
                        require ext_call.success
                        require etherRaised + msg.value >= etherRaised
                        require etherRaised + msg.value >= msg.value
                        etherRaised += msg.value
                        call melonportAddress with:
                           value msg.value wei
                             gas 2300 * is_zero(value) wei
                        require ext_call.success
                        emit TokensBought(msg.value, 2150 * msg.value / 1000, arg1);
                    else:
                        require startTime + (504 * 24 * 3600) <= block.timestamp
                        require block.timestamp < endTime
                        if msg.value:
                            require msg.value
                            require 2100 * msg.value / msg.value == 2100
                        require ext_code.size(melonTokenAddress)
                        call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                             gas gas_remaining - 50 wei
                            args address(arg1), 2100 * msg.value / 1000
                        require ext_call.success
                        require etherRaised + msg.value >= etherRaised
                        require etherRaised + msg.value >= msg.value
                        etherRaised += msg.value
                        call melonportAddress with:
                           value msg.value wei
                             gas 2300 * is_zero(value) wei
                        require ext_call.success
                        emit TokensBought(msg.value, 2100 * msg.value / 1000, arg1);
            else:
                if block.timestamp < startTime + (336 * 24 * 3600):
                    if msg.value:
                        require msg.value
                        require 2200 * msg.value / msg.value == 2200
                    require ext_code.size(melonTokenAddress)
                    call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                         gas gas_remaining - 50 wei
                        args address(arg1), 2200 * msg.value / 1000
                    require ext_call.success
                    require etherRaised + msg.value >= etherRaised
                    require etherRaised + msg.value >= msg.value
                    etherRaised += msg.value
                    call melonportAddress with:
                       value msg.value wei
                         gas 2300 * is_zero(value) wei
                    require ext_call.success
                    emit TokensBought(msg.value, 2200 * msg.value / 1000, arg1);
                else:
                    if startTime + (336 * 24 * 3600) > block.timestamp:
                        require startTime + (504 * 24 * 3600) <= block.timestamp
                        require block.timestamp < endTime
                        if msg.value:
                            require msg.value
                            require 2100 * msg.value / msg.value == 2100
                        require ext_code.size(melonTokenAddress)
                        call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                             gas gas_remaining - 50 wei
                            args address(arg1), 2100 * msg.value / 1000
                        require ext_call.success
                        require etherRaised + msg.value >= etherRaised
                        require etherRaised + msg.value >= msg.value
                        etherRaised += msg.value
                        call melonportAddress with:
                           value msg.value wei
                             gas 2300 * is_zero(value) wei
                        require ext_call.success
                        emit TokensBought(msg.value, 2100 * msg.value / 1000, arg1);
                    else:
                        if block.timestamp < startTime + (504 * 24 * 3600):
                            if msg.value:
                                require msg.value
                                require 2150 * msg.value / msg.value == 2150
                            require ext_code.size(melonTokenAddress)
                            call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                                 gas gas_remaining - 50 wei
                                args address(arg1), 2150 * msg.value / 1000
                            require ext_call.success
                            require etherRaised + msg.value >= etherRaised
                            require etherRaised + msg.value >= msg.value
                            etherRaised += msg.value
                            call melonportAddress with:
                               value msg.value wei
                                 gas 2300 * is_zero(value) wei
                            require ext_call.success
                            emit TokensBought(msg.value, 2150 * msg.value / 1000, arg1);
                        else:
                            require startTime + (504 * 24 * 3600) <= block.timestamp
                            require block.timestamp < endTime
                            if msg.value:
                                require msg.value
                                require 2100 * msg.value / msg.value == 2100
                            require ext_code.size(melonTokenAddress)
                            call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                                 gas gas_remaining - 50 wei
                                args address(arg1), 2100 * msg.value / 1000
                            require ext_call.success
                            require etherRaised + msg.value >= etherRaised
                            require etherRaised + msg.value >= msg.value
                            etherRaised += msg.value
                            call melonportAddress with:
                               value msg.value wei
                                 gas 2300 * is_zero(value) wei
                            require ext_call.success
                            emit TokensBought(msg.value, 2100 * msg.value / 1000, arg1);
}

function buy(uint8 arg1, bytes32 arg2, bytes32 arg3) payable {
    hash = sha256hash(msg.sender) 
    require sha256hash.result
    signer = erecover(hash, arg1 << 248, arg2, arg3) 
    require erecover.result
    require address(signer) == signerAddress
    require block.timestamp >= startTime
    require block.timestamp < endTime
    require not stor7
    require etherRaised + msg.value >= etherRaised
    require etherRaised + msg.value >= msg.value
    require etherRaised + msg.value <= 220000 * 10^18
    if startTime > block.timestamp:
        if startTime + (168 * 24 * 3600) > block.timestamp:
            if startTime + (336 * 24 * 3600) > block.timestamp:
                require startTime + (504 * 24 * 3600) <= block.timestamp
                require block.timestamp < endTime
                if msg.value:
                    require msg.value
                    require 2100 * msg.value / msg.value == 2100
                require ext_code.size(melonTokenAddress)
                call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                     gas gas_remaining - 50 wei
                    args msg.sender, 2100 * msg.value / 1000
                require ext_call.success
                require etherRaised + msg.value >= etherRaised
                require etherRaised + msg.value >= msg.value
                etherRaised += msg.value
                call melonportAddress with:
                   value msg.value wei
                     gas 2300 * is_zero(value) wei
                require ext_call.success
                emit TokensBought(msg.value, 2100 * msg.value / 1000, msg.sender);
            else:
                if block.timestamp < startTime + (504 * 24 * 3600):
                    if msg.value:
                        require msg.value
                        require 2150 * msg.value / msg.value == 2150
                    require ext_code.size(melonTokenAddress)
                    call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                         gas gas_remaining - 50 wei
                        args msg.sender, 2150 * msg.value / 1000
                    require ext_call.success
                    require etherRaised + msg.value >= etherRaised
                    require etherRaised + msg.value >= msg.value
                    etherRaised += msg.value
                    call melonportAddress with:
                       value msg.value wei
                         gas 2300 * is_zero(value) wei
                    require ext_call.success
                    emit TokensBought(msg.value, 2150 * msg.value / 1000, msg.sender);
                else:
                    require startTime + (504 * 24 * 3600) <= block.timestamp
                    require block.timestamp < endTime
                    if msg.value:
                        require msg.value
                        require 2100 * msg.value / msg.value == 2100
                    require ext_code.size(melonTokenAddress)
                    call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                         gas gas_remaining - 50 wei
                        args msg.sender, 2100 * msg.value / 1000
                    require ext_call.success
                    require etherRaised + msg.value >= etherRaised
                    require etherRaised + msg.value >= msg.value
                    etherRaised += msg.value
                    call melonportAddress with:
                       value msg.value wei
                         gas 2300 * is_zero(value) wei
                    require ext_call.success
                    emit TokensBought(msg.value, 2100 * msg.value / 1000, msg.sender);
        else:
            if block.timestamp < startTime + (336 * 24 * 3600):
                if msg.value:
                    require msg.value
                    require 2200 * msg.value / msg.value == 2200
                require ext_code.size(melonTokenAddress)
                call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                     gas gas_remaining - 50 wei
                    args msg.sender, 2200 * msg.value / 1000
                require ext_call.success
                require etherRaised + msg.value >= etherRaised
                require etherRaised + msg.value >= msg.value
                etherRaised += msg.value
                call melonportAddress with:
                   value msg.value wei
                     gas 2300 * is_zero(value) wei
                require ext_call.success
                emit TokensBought(msg.value, 2200 * msg.value / 1000, msg.sender);
            else:
                if startTime + (336 * 24 * 3600) > block.timestamp:
                    require startTime + (504 * 24 * 3600) <= block.timestamp
                    require block.timestamp < endTime
                    if msg.value:
                        require msg.value
                        require 2100 * msg.value / msg.value == 2100
                    require ext_code.size(melonTokenAddress)
                    call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                         gas gas_remaining - 50 wei
                        args msg.sender, 2100 * msg.value / 1000
                    require ext_call.success
                    require etherRaised + msg.value >= etherRaised
                    require etherRaised + msg.value >= msg.value
                    etherRaised += msg.value
                    call melonportAddress with:
                       value msg.value wei
                         gas 2300 * is_zero(value) wei
                    require ext_call.success
                    emit TokensBought(msg.value, 2100 * msg.value / 1000, msg.sender);
                else:
                    if block.timestamp < startTime + (504 * 24 * 3600):
                        if msg.value:
                            require msg.value
                            require 2150 * msg.value / msg.value == 2150
                        require ext_code.size(melonTokenAddress)
                        call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                             gas gas_remaining - 50 wei
                            args msg.sender, 2150 * msg.value / 1000
                        require ext_call.success
                        require etherRaised + msg.value >= etherRaised
                        require etherRaised + msg.value >= msg.value
                        etherRaised += msg.value
                        call melonportAddress with:
                           value msg.value wei
                             gas 2300 * is_zero(value) wei
                        require ext_call.success
                        emit TokensBought(msg.value, 2150 * msg.value / 1000, msg.sender);
                    else:
                        require startTime + (504 * 24 * 3600) <= block.timestamp
                        require block.timestamp < endTime
                        if msg.value:
                            require msg.value
                            require 2100 * msg.value / msg.value == 2100
                        require ext_code.size(melonTokenAddress)
                        call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                             gas gas_remaining - 50 wei
                            args msg.sender, 2100 * msg.value / 1000
                        require ext_call.success
                        require etherRaised + msg.value >= etherRaised
                        require etherRaised + msg.value >= msg.value
                        etherRaised += msg.value
                        call melonportAddress with:
                           value msg.value wei
                             gas 2300 * is_zero(value) wei
                        require ext_call.success
                        emit TokensBought(msg.value, 2100 * msg.value / 1000, msg.sender);
    else:
        if block.timestamp < startTime + (168 * 24 * 3600):
            if msg.value:
                require msg.value
                require 2250 * msg.value / msg.value == 2250
            require ext_code.size(melonTokenAddress)
            call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                 gas gas_remaining - 50 wei
                args msg.sender, 2250 * msg.value / 1000
            require ext_call.success
            require etherRaised + msg.value >= etherRaised
            require etherRaised + msg.value >= msg.value
            etherRaised += msg.value
            call melonportAddress with:
               value msg.value wei
                 gas 2300 * is_zero(value) wei
            require ext_call.success
            emit TokensBought(msg.value, 2250 * msg.value / 1000, msg.sender);
        else:
            if startTime + (168 * 24 * 3600) > block.timestamp:
                if startTime + (336 * 24 * 3600) > block.timestamp:
                    require startTime + (504 * 24 * 3600) <= block.timestamp
                    require block.timestamp < endTime
                    if msg.value:
                        require msg.value
                        require 2100 * msg.value / msg.value == 2100
                    require ext_code.size(melonTokenAddress)
                    call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                         gas gas_remaining - 50 wei
                        args msg.sender, 2100 * msg.value / 1000
                    require ext_call.success
                    require etherRaised + msg.value >= etherRaised
                    require etherRaised + msg.value >= msg.value
                    etherRaised += msg.value
                    call melonportAddress with:
                       value msg.value wei
                         gas 2300 * is_zero(value) wei
                    require ext_call.success
                    emit TokensBought(msg.value, 2100 * msg.value / 1000, msg.sender);
                else:
                    if block.timestamp < startTime + (504 * 24 * 3600):
                        if msg.value:
                            require msg.value
                            require 2150 * msg.value / msg.value == 2150
                        require ext_code.size(melonTokenAddress)
                        call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                             gas gas_remaining - 50 wei
                            args msg.sender, 2150 * msg.value / 1000
                        require ext_call.success
                        require etherRaised + msg.value >= etherRaised
                        require etherRaised + msg.value >= msg.value
                        etherRaised += msg.value
                        call melonportAddress with:
                           value msg.value wei
                             gas 2300 * is_zero(value) wei
                        require ext_call.success
                        emit TokensBought(msg.value, 2150 * msg.value / 1000, msg.sender);
                    else:
                        require startTime + (504 * 24 * 3600) <= block.timestamp
                        require block.timestamp < endTime
                        if msg.value:
                            require msg.value
                            require 2100 * msg.value / msg.value == 2100
                        require ext_code.size(melonTokenAddress)
                        call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                             gas gas_remaining - 50 wei
                            args msg.sender, 2100 * msg.value / 1000
                        require ext_call.success
                        require etherRaised + msg.value >= etherRaised
                        require etherRaised + msg.value >= msg.value
                        etherRaised += msg.value
                        call melonportAddress with:
                           value msg.value wei
                             gas 2300 * is_zero(value) wei
                        require ext_call.success
                        emit TokensBought(msg.value, 2100 * msg.value / 1000, msg.sender);
            else:
                if block.timestamp < startTime + (336 * 24 * 3600):
                    if msg.value:
                        require msg.value
                        require 2200 * msg.value / msg.value == 2200
                    require ext_code.size(melonTokenAddress)
                    call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                         gas gas_remaining - 50 wei
                        args msg.sender, 2200 * msg.value / 1000
                    require ext_call.success
                    require etherRaised + msg.value >= etherRaised
                    require etherRaised + msg.value >= msg.value
                    etherRaised += msg.value
                    call melonportAddress with:
                       value msg.value wei
                         gas 2300 * is_zero(value) wei
                    require ext_call.success
                    emit TokensBought(msg.value, 2200 * msg.value / 1000, msg.sender);
                else:
                    if startTime + (336 * 24 * 3600) > block.timestamp:
                        require startTime + (504 * 24 * 3600) <= block.timestamp
                        require block.timestamp < endTime
                        if msg.value:
                            require msg.value
                            require 2100 * msg.value / msg.value == 2100
                        require ext_code.size(melonTokenAddress)
                        call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                             gas gas_remaining - 50 wei
                            args msg.sender, 2100 * msg.value / 1000
                        require ext_call.success
                        require etherRaised + msg.value >= etherRaised
                        require etherRaised + msg.value >= msg.value
                        etherRaised += msg.value
                        call melonportAddress with:
                           value msg.value wei
                             gas 2300 * is_zero(value) wei
                        require ext_call.success
                        emit TokensBought(msg.value, 2100 * msg.value / 1000, msg.sender);
                    else:
                        if block.timestamp < startTime + (504 * 24 * 3600):
                            if msg.value:
                                require msg.value
                                require 2150 * msg.value / msg.value == 2150
                            require ext_code.size(melonTokenAddress)
                            call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                                 gas gas_remaining - 50 wei
                                args msg.sender, 2150 * msg.value / 1000
                            require ext_call.success
                            require etherRaised + msg.value >= etherRaised
                            require etherRaised + msg.value >= msg.value
                            etherRaised += msg.value
                            call melonportAddress with:
                               value msg.value wei
                                 gas 2300 * is_zero(value) wei
                            require ext_call.success
                            emit TokensBought(msg.value, 2150 * msg.value / 1000, msg.sender);
                        else:
                            require startTime + (504 * 24 * 3600) <= block.timestamp
                            require block.timestamp < endTime
                            if msg.value:
                                require msg.value
                                require 2100 * msg.value / msg.value == 2100
                            require ext_code.size(melonTokenAddress)
                            call melonTokenAddress.mintLiquidToken(address rg1, uint256 rg2) with:
                                 gas gas_remaining - 50 wei
                                args msg.sender, 2100 * msg.value / 1000
                            require ext_call.success
                            require etherRaised + msg.value >= etherRaised
                            require etherRaised + msg.value >= msg.value
                            etherRaised += msg.value
                            call melonportAddress with:
                               value msg.value wei
                                 gas 2300 * is_zero(value) wei
                            require ext_call.success
                            emit TokensBought(msg.value, 2100 * msg.value / 1000, msg.sender);
}



}
